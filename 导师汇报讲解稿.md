# 导师组会汇报讲解稿（20分钟主讲版）

这份稿子按 `10` 张主PPT来写，默认总时长控制在 `18` 到 `20` 分钟。整篇汇报只抓一条主线：

**先查准，再分析，再做原型闭环。**

全程统一口径：

- 不是让大语言模型直接替代人工分析。
- 而是利用大语言模型的语义理解能力，在领域知识注入、结构化规划、分层协同和规则化结果组织下，完成面向政务数据的可靠查询与规范分析。
- 第五章只作为原型系统和方法闭环验证，不主动讲成真实大规模生产部署。

---

## 开场 30 秒

老师好，我这次汇报的题目是面向政务场景的智能问数与数据分析研究。整篇论文我想围绕一个核心问题来讲，就是在政务数据已经大量沉淀的情况下，如何让非技术人员基于自然语言，完成从可靠查数到规范分析的完整过程。我的整体思路不是让大模型端到端自由生成全部结果，而是按照“先查准，再分析，再做原型闭环”的路径，逐层解决问题。

---

## 第1页：研究背景与问题（1.5分钟）

**一句话版：** 政务数据不是没有，而是难以被非技术人员直接、规范地使用。

**展开讲法：**

这一页我先讲研究背景。当前政务系统里已经积累了大量结构化数据，但真正使用这些数据时，常见模式还是两种：一种是业务人员提需求、技术人员写 SQL、再返回结果；另一种是依赖固定报表和 BI 看板。前一种门槛高、周期长，后一种灵活性不足、分析深度有限。

所以我把问题概括成三个词：**查数难、分析难、协同难。** 也就是说，数据虽然沉淀下来了，但从“有数据”到“能自然语言问数、查数、分析数”，中间还有一条并没有被真正打通的链路。我的论文就是围绕这条链路展开的。

**转场句：**

如果进一步看这个问题，核心难点并不只是“把一句话变成 SQL”，而是查询、分析和两者协同三个层面同时都存在障碍。

---

## 第2页：核心挑战（2分钟）

**一句话版：** 难点不在单点生成，而在于领域知识、复杂查询和分析协同三方面同时约束。

**展开讲法：**

我把核心挑战概括为四点。

第一，**政务术语和物理字段之间存在明显语义鸿沟。** 业务里说的是“预算执行率”“同比增长率”，底层字段可能是缩写字段，很多指标还不是直接存储，而是隐含口径和计算公式。

第二，**政务查询复杂度呈长尾分布。** 有些问题是高频、标准化的，有些则涉及多表关联、复杂聚合甚至嵌套子查询，单一生成策略很难同时兼顾。

第三，**分析需求本身带有多重隐含约束。** 用户说“帮我分析一下”，背后往往同时包含指标、维度、时间粒度、统计口径和展示形式要求，直接端到端生成很容易漏步骤或者结论漂移。

第四，**查询与分析职责如果耦合不清，错误会跨层传播。** 如果分析环节又重新自由写 SQL，那查询阶段已经解决的问题会再次暴露；如果两者完全割裂，分析又拿不到足够的字段语义和粒度信息。

所以这篇论文真正要解决的，不是单点的 SQL 生成，而是如何在领域知识深度融入的前提下，把查询正确性、分析规范性和两者之间的协同贯通起来。

**转场句：**

基于这些挑战，我没有把问题做成一个“大模型端到端全包”的方案，而是拆成一个两层递进的技术路线。

---

## 第3页：总体思路（1.5分钟）

**一句话版：** 整篇论文按照“可靠查询 -> 规范分析 -> 原型闭环”的链路展开。

**展开讲法：**

我的总体思路可以概括成三步。

第一步，先解决**可靠查询**，也就是先把“数查对”。如果底层数据拿不准，后续分析就没有意义。

第二步，在可靠取数基础上做**规范分析**。也就是不是让模型直接自由写一段分析，而是先把分析需求规划清楚，再把查询层和分析层各自放到最适合的位置上执行。

第三步，再把前两章的方法组织成一个**原型系统闭环**，验证从自然语言输入、取数、分析到结果追溯这一整条链路是可交互、可展示、可复核的。

所以第三章回答的是“怎么查准”，第四章回答的是“怎么规范分析”，第五章回答的是“怎样把前两章方法组织成原型闭环”。

**转场句：**

下面我先汇报第一个研究点，也就是面向政务场景的可靠查询生成方法。

---

## 第4页：研究点一方法（2.5分钟）

**一句话版：** 第三章的核心不是让模型猜 SQL，而是给模型一个更可靠的领域知识增强和候选筛选过程。

**展开讲法：**

第三章我主要做了四件事。

第一，构建了三类离线知识支撑：**向量库、政务领域知识库和动态样例库。** 向量库主要支持值检索，知识库显式保存业务术语映射、逻辑计算公式和值域约束，动态样例库则提供与当前问题相近的少样本推理参考。

第二，我提出了 **LA-Schema**。它不是只给模型原始数据库模式，而是把业务术语映射、逻辑公式和值域约束显式注入模式表示，让模型在生成 SQL 时不只是依赖隐式推理，而是可以调用结构化先验。

第三，我设计了**双路径异构生成策略**。逻辑映射生成器更适合处理高频、标准化的业务指标查询；渐进分治生成器更适合处理跨表关联、复杂聚合和长尾复杂查询。

第四，在候选生成之后，我加入了**执行反馈修复**和**候选判别模型**。前者负责把语法或执行错误的 SQL 修一遍，后者负责从多个候选中选出最优 SQL，而不是直接依赖多数投票。

这一章的本质可以概括成一句话：**不是让大模型自由猜，而是让它在领域知识增强、异构生成、执行修复和判别选择的约束下，更稳地生成正确 SQL。**

**转场句：**

这套方法是否真的有效，关键要看实验结果。

---

## 第5页：研究点一结果（2分钟）

**一句话版：** 第三章方法在政务场景上显著优于对比方法，说明领域知识增强和异构协同确实有效。

**展开讲法：**

在查询实验上，我构建了自建政务数据集 **GovSQL**，它包含 `13` 个数据库、`92` 张数据表、`563` 个字段和 `1368` 条自然语言问题-SQL 对，其中复杂查询占比 `38.5%`。

在这个数据集上，本文方法的执行准确率达到 **91.47%**，而最强基线 **XiYan-SQL** 是 **83.83%**，提升了 **7.64** 个百分点。

从消融实验看，去掉 **LA-Schema** 后准确率降到 **84.62%**，去掉 **Selection Agent** 后降到 **86.12%**。这说明第三章的提升不是来自某一个小技巧，而是来自领域知识增强、异构候选生成和判别选择这几个环节的协同。

此外，在公共数据集上，本文方法在 **BIRD** 上达到 **71.23%**，在 **Spider** 上达到 **89.25%**，说明它不只是对自建政务数据集有效，在通用场景下也保持了较强竞争力。

**转场句：**

但只解决查数还不够，因为真实业务里用户通常不是只想拿一张结果表，而是还想继续做趋势、对比、排名和结构分析。

---

## 第6页：研究点二方法（2.5分钟）

**一句话版：** 第四章的核心是把开放式分析需求先规划清楚，再把查询层和分析层分开执行。

**展开讲法：**

第四章的第一项核心设计是 **AP-Schema**。它把自然语言分析需求转化成结构化分析规划，明确分析类型、指标、维度、筛选条件、统计粒度、查询层操作、分析层操作、可视化规格和摘要要求。

第二项核心设计是**查询增强的任务分解机制**。我把分析任务拆成两层：涉及统计口径、复杂关联和业务逻辑计算的部分，下沉给第三章查询模块；排序、占比、透视、标注和结果格式化这些轻量操作，留给分析层处理。

第三项核心设计是**轻量分析算子链和可视化规则**。也就是说，分析层不是任由模型自由发挥，而是在查询结果之上，用规则化算子链去组织图表、表格和文字洞察，并且对文字洞察采用“先抽取事实，再组织文本”的方式，尽量抑制结论漂移。

这一章的本质可以概括成一句话：**不是让模型直接写一段分析，而是先把分析需求规划成可执行结构，再把查询和分析放在合适层级上协同执行。**

**转场句：**

这一思路是否成立，同样要看第四章实验结果。

---

## 第7页：研究点二结果（2分钟）

**一句话版：** 第四章方法在分析稳定性、结果正确率和图表规范性上都明显优于对比方法。

**展开讲法：**

第四章实验基于同源政务场景中的 `5` 个数据库、`33` 张数据表，构建了 `168` 个自然语言分析任务，其中包括趋势、对比、排名和结构四类典型分析。

在这些任务上，本文方法的**流程成功率**达到 **92.9%**，**任务完成率**达到 **85.7%**，**结果正确率**达到 **95.8%**，**图表恰当性**达到 **4.6** 分，整体都明显优于 Code-Gen Agent、ReAct Agent、Direct-Analysis 和 Plan+Free-SQL。

如果单看规划质量，AP-Schema 的**意图识别准确率**是 **94.6%**，**槽位填充完备率**是 **91.8%**，**约束满足率**是 **91.1%**。这说明分析阶段的提升，并不是因为“模型更会说”，而是因为先做了结构化规划，再在可靠取数基础上做规范结果生成。

从消融实验也能看到，去掉任务分解后，结果正确率从 **95.8%** 降到 **86.4%**；去掉事实抽取后，任务完成率和结果正确率也都会下降。这进一步说明第四章的关键在于结构化规划、分层协同和规则化结果组织。

**转场句：**

前两章解决的是方法问题，第五章我把它们组织成一个原型闭环来验证整个链路能否串起来。

---

## 第8页：系统闭环（1.5分钟）

**一句话版：** 第五章的定位是原型系统和方法闭环验证，而不是单独再提出一个新的方法点。

**展开讲法：**

这一章我建议讲得收敛一些。我的表述重点是：在第三章和第四章方法基础上，我设计并实现了一个面向政务场景的智能问数原型系统，用来验证从自然语言输入、可靠取数、规范分析到结果追溯这条链路是可以被统一组织起来的。

系统采用四层架构：表示层、应用层、智能服务层和数据层。应用层用 FastAPI 作为统一接口入口，智能服务层用 LangGraph 组织查询子图和分析子图，数据层用 PostgreSQL、Milvus、Redis 等组件分别承接元数据、向量索引、缓存与事件通道。

这里最关键的不是把工程细节讲得多深，而是说明：**分析请求并不是绕过查询服务直接生成图表，而是先生成 AP-Schema，再投影查询约束，复用第三章查询能力取数，最后在分析层完成图表、表格和洞察组织，并把过程和结果可追溯地保存下来。**

**转场句：**

如果把前三部分收束一下，这篇论文真正的贡献可以概括为四个层面。

---

## 第9页：创新点与贡献（1.5分钟）

**一句话版：** 本文的贡献不是单点指标优化，而是围绕政务问数建立了一条完整、可验证的技术链路。

**展开讲法：**

我把创新点概括成四层。

第一，**查询层创新**。提出 LA-Schema，把业务术语映射、逻辑公式和值域约束显式注入模式表示。

第二，**生成层创新**。提出逻辑映射生成器与渐进分治生成器的异构协同机制，并结合执行反馈修复和候选判别模型，提高复杂政务查询的准确性和鲁棒性。

第三，**分析层创新**。提出 AP-Schema 和查询增强的任务分解机制，明确查询层和分析层职责边界，形成“先查准、再分析”的协同路径。

第四，**系统层贡献**。完成原型系统实现，把查询方法和分析方法组织成一个可交互、可追溯的统一闭环。

如果用一句话来概括，就是：**我这篇论文不是只做了一个查询模型，也不是只做了一个分析 Agent，而是面向政务场景，把自然语言问数这件事从可靠查询、规范分析到原型闭环打通了。**

**转场句：**

最后我简单汇报一下这项工作的不足和下一步方向。

---

## 第10页：不足与展望（1.5分钟）

**一句话版：** 当前工作已经形成完整技术链路，但验证范围、知识维护和复杂场景覆盖仍有限。

**展开讲法：**

这篇论文也有比较明确的边界。

第一，当前数据和任务验证范围主要还是**同源政务场景**，虽然能反映财政预算、公共资源和项目管理等典型需求，但跨地区、跨部门和跨制度口径的泛化能力还需要进一步验证。

第二，领域知识库、动态样例库以及部分标注工作，当前仍然存在一定**人工维护成本**，未来需要进一步研究更自动化的知识获取和更新机制。

第三，第四章当前重点覆盖的是趋势、对比、排名和结构四类典型分析任务，对于多轮深度分析、预测分析、权限控制和更复杂的真实部署治理，仍然属于后续拓展方向。

所以我的收束是：**本文已经把面向政务场景的“可靠查询 + 规范分析 + 原型闭环”这条主线做完整了，但更大范围的泛化验证和更复杂场景落地，仍然是下一步工作。**

---

## 结束收束 20 秒

我的汇报就到这里。总体上，这篇论文的核心不是把大语言模型直接用于自由问数，而是在政务场景下，通过领域知识增强、结构化规划、分层协同和规则化结果组织，构建了一条从可靠查询到规范分析再到原型闭环的完整技术链路。请老师批评指正。

---

## 如果只剩 3 分钟，压缩成这 4 句

1. 这篇论文研究的是政务场景下如何让非技术人员用自然语言完成可靠查数和规范分析。
2. 第三章通过 LA-Schema、双生成器、执行反馈修复和候选判别，把 GovSQL 上的执行准确率做到 `91.47%`，比 XiYan-SQL 高 `7.64` 个百分点。
3. 第四章通过 AP-Schema、查询增强任务分解和轻量分析算子，把 `168` 个分析任务上的结果正确率做到 `95.8%`，任务完成率做到 `85.7%`。
4. 第五章把前两章方法组织成原型系统，证明从自然语言输入到可追溯分析输出的链路能够被统一实现。
