# 给导师汇报的论文讲解稿

## 一句话概括

本文面向政务数据场景，研究如何利用大语言模型，把用户的自然语言需求贯通为“可靠查询 + 规范分析 + 系统落地”的完整链路，目标是让非技术人员也能直接问数、查数、分析数，并获得可追溯、可解释的结果。

---

## 1. 研究意义、研究挑战、研究内容

### 1.1 研究意义

我这篇论文的现实背景是，政务系统里已经积累了大量结构化数据，但这些数据真正被业务人员高效使用起来并不容易。现在常见的方式，要么是业务人员提需求、技术人员写 SQL、再返回结果；要么是依赖固定报表和 BI 看板。这两种方式都存在问题：前者周期长、门槛高，后者灵活性差、分析深度不足。

所以，这篇论文的核心意义在于两个层面：

第一，是实践意义。它希望降低政务人员用数的技术门槛，把“提需求-等排期-拿结果”的模式，转变成“自然语言直接问数、系统自动分析”的模式，从而提升数据支撑决策的效率。

第二，是理论意义。它不是只做一个应用系统，而是围绕政务场景，提出了两类方法：一类是领域知识增强的 Text-to-SQL 方法，解决“怎么查准”；另一类是基于分析规划与查询增强的数据分析方法，解决“怎么在查准的基础上做规范分析”。这实际上是在补足大模型从查询到分析之间的中间链路。

### 1.2 研究挑战

这篇论文面对的挑战可以从“查询、分析、协同”三个层面来理解：

**挑战一：政务领域的语义鸿沟导致查询生成困难。**
政务术语和数据库字段之间存在很强的语义鸿沟。业务上说的是“预算执行率”“同比增长率”，数据库里可能是非常晦涩的缩写字段（如 `ybsr`），模型很难直接对上。而且很多政务指标不是表里直接存的，而是带有业务口径和计算规则的隐含逻辑，比如“同比增长率”需要跨年取值再做除法。如果仅靠大模型自由生成，很容易出现“SQL 语法对了，但业务逻辑错了”的问题——我把它叫做“逻辑幻觉”。这个问题的根本原因是：领域知识的边界远超数据库模式本身能表达的信息范围，而现有方法缺乏将这些先验知识系统性注入生成流程的机制。

**挑战二：政务查询的复杂度分布呈长尾特征，单一生成策略难以覆盖。**
政务查询既有高频、标准化的问题（如某个指标的简单汇总筛选），也有低频、复杂的长尾问题（如多层嵌套子查询、跨表关联聚合）。对简单查询用过重的推理策略会引入冗余和出错风险，对复杂查询用直接映射式的方法又捕获不了深层逻辑。这意味着需要一种能够自适应匹配不同复杂度需求的异构生成机制。

**挑战三：分析需求隐含多重约束，端到端自由生成缺乏可控性。**
用户在真实场景中往往不只是想“查一个数”，而是想做趋势追踪、横向对比、排名识别、结构刻画等进一步分析。一句看似简单的分析需求背后，实际隐含了指标、维度、时间粒度、统计口径和展示形式等多重约束。如果让大模型端到端自由生成分析，很容易出现两个问题：一是关键分析步骤遗漏（比如忘了做占比计算或忽略了时间粒度），二是文字结论中编造不存在的数值（结论漂移）。

**挑战四：查询与分析的职责耦合，错误容易跨层传播。**
如果分析模块在生成结果时又重新去写 SQL，那第三章已经解决的查询问题就会被再次暴露——字段误解、口径偏差、连接错误等风险重新出现。反过来，如果查询和分析完全割裂，分析模块拿不到足够的元数据（字段含义、单位、粒度），图表映射和摘要生成又会缺乏依据。这意味着查询和分析之间需要一种既清晰分工又紧密衔接的协同机制，而不是简单的串联或一体化。

一句话概括：**核心困难不是单点的“生成 SQL”或“做分析”，而是如何在领域知识深度融入的前提下，把查询的正确性、分析的规范性和两者之间的协同贯通为一条可控、可验证的完整链路。**

### 1.3 研究内容

围绕这些挑战，论文主要做了三项工作：

1. 研究基于逻辑增强与异构协同的政务数据查询生成方法，解决政务场景下自然语言到 SQL 的可靠映射问题。
2. 研究基于分析规划与查询增强的政务数据分析方法，解决自然语言分析需求到规范化分析结果的转化问题。
3. 在前两项方法基础上，设计并实现了一个政务智能问数系统，把方法真正落到可交互、可追溯的系统中。

如果用一句更凝练的话来说，整篇论文就是在回答一个问题：**如何让政务人员用自然语言完成从问数到分析的完整过程，而且结果要尽可能可靠、规范、可落地。**

---

## 2. 章节安排

整篇论文可以按“提出问题 - 解决查询 - 解决分析 - 系统落地”来理解。

### 第一章 绪论

这一章主要说明为什么要做这个题目。先讲数字政府建设背景下政务数据越积越多，但实际用起来仍然门槛高、效率低、分析浅；然后梳理 Text-to-SQL、自动数据分析和政务数据应用平台三方面的研究现状，进一步提出本文要解决的三个核心问题：领域知识怎么融入查询、分析需求怎么结构化、查询和分析怎么协同落地。

### 第二章 相关理论与技术

这一章是技术基础铺垫，主要介绍大语言模型、Text-to-SQL、Text-to-Analysis，以及多智能体协同等相关理论，为后面的方法章节做准备。

### 第三章 基于逻辑增强与异构协同的政务数据查询生成方法

这一章是第一个核心研究点，重点解决“查准”的问题。主要做法包括：构建领域知识库和动态样例库，提出逻辑增强模式 LA-Schema，把业务术语映射、计算公式和值域约束显式注入模式表示；然后设计双路径异构生成策略、执行反馈修复机制和候选判别模型，提高 SQL 生成的准确性和稳定性。

### 第四章 基于分析规划与查询增强的政务数据分析方法

这一章是第二个核心研究点，重点解决“分析规范”的问题。主要做法包括：提出分析规划模式 AP-Schema，把自然语言分析需求转化成结构化规划；再通过查询增强的任务分解机制，把任务分成查询层和分析层，复用第三章的可靠取数能力；最后通过轻量分析算子和可视化规则，生成标准化的图表、表格和文字洞察。

### 第五章 政务智能问数系统设计与实现

这一章对应工程落地。它不是单独的新方法点，而是把前两章的方法整合成一个完整系统，说明从前端交互、服务编排到数据存储和结果追溯，是如何实现自然语言问数和分析闭环的。

### 第六章 结论与展望

这一章总结全文工作，并对后续可继续拓展的方向做展望。

如果向导师更口语化地概括章节安排，可以这样讲：

“第一章提出问题，第二章补技术基础，第三章解决查数准不准，第四章解决分析规不规范，第五章把前两章整合成系统，第六章做总结和展望。”

---

## 3. 两个研究点，怎么串起来，故事怎么讲

### 3.1 第一个研究点：面向政务场景的可靠查询

第一个研究点的核心目标，是解决自然语言到 SQL 的可靠映射问题，也就是先把“数查对”。

这一部分我重点做了四件事：

1. 构建政务领域知识支撑，包括向量库、领域知识库和动态样例库。
2. 提出 LA-Schema，把业务术语映射、逻辑计算公式和值域约束显式注入数据库模式，缓解政务场景下术语歧义和逻辑幻觉问题。
3. 提出异构协同生成策略，用逻辑映射生成器处理高频、标准化问题，用渐进分治生成器处理复杂长尾问题。
4. 设计执行反馈修复机制和候选判别模型，从多个 SQL 候选里筛出最优结果，提升整体鲁棒性。

这一研究点的本质，可以概括成一句话：**不是让大模型“猜”SQL，而是给它提供结构化的领域知识和更可靠的生成-修复-判别流程，让它更稳地生成正确 SQL。**

从实验结果上看，这一方法在自建政务数据集上执行准确率达到 91.47%，相比最强基线还有明显提升，说明它对政务场景确实有效。

### 3.2 第二个研究点：面向政务场景的规范分析

第二个研究点的核心目标，是在“查准”的基础上，进一步解决“怎么规范分析”的问题。

因为真实业务里，用户通常不是只想拿一张结果表，而是想进一步回答这些问题：趋势如何、哪里差异最大、排名靠前的是谁、结构占比怎样。这个时候如果还是让大模型端到端自由生成，很容易出两个问题：一是分析步骤缺失，二是分析时又重新去自由写 SQL，导致查询错误再次暴露。

所以这一研究点主要做了三件事：

1. 提出 AP-Schema，把自然语言分析需求转化成结构化分析规划，明确分析类型、指标、维度、时间粒度、查询层操作、分析层操作、可视化规格和摘要要求。
2. 提出查询增强的任务分解机制，把分析任务拆成两层：涉及业务口径、复杂计算、跨表关联的部分交给第三章查询模块，排序、占比、透视、标注等轻量操作留在分析层处理。
3. 引入分析算子链和可视化规则，生成标准化的图表、表格和文字洞察，避免大模型在结果组织阶段过度自由发挥。

这一研究点的本质也可以概括成一句话：**不是让大模型直接“写一段分析”，而是先把分析需求规划清楚，再把查询和分析各自放到最适合的位置上执行。**

从实验结果看，在 56 个政务分析任务上，本文方法的流程成功任务数、任务完成数和正确分析结果数都明显优于对比方法，说明这种“规划 + 查询增强 + 算子生成”的思路更稳定。

### 3.3 两个研究点是怎么串起来的

这两个研究点不是并列的，而是前后递进、层层展开的关系。

最核心的逻辑是：

**如果底层查询不可靠，那么上层分析就没有意义；如果只能查数不能分析，那系统的价值又停留在“取数工具”层面。**

所以，第三章先解决“查准”，第四章再解决“分析规范”，两者共同构成一条从自然语言需求到数据洞察输出的完整技术链。

可以把它理解成这样一条主线：

1. 用户先提出自然语言需求。
2. 第三章负责把这个需求可靠地落到数据库，得到可信的数据结果。
3. 第四章在可信数据基础上，把需求转成结构化分析规划，再生成规范的分析结果。
4. 第五章再把这两部分组织成一个可交互、可追溯的系统。

也就是说，第三章解决的是“数据正确性”，第四章解决的是“结果表达性和规范性”，第五章解决的是“工程可落地性”。

### 3.4 这篇论文的“故事”怎么讲

我建议向导师汇报时，按下面这条故事线来讲：

#### 第一层：先讲现实问题

政务数据很多，但真正使用时存在三个痛点：一是查数门槛高，二是分析不灵活，三是结果缺乏规范化组织。传统方式要么靠人工写 SQL，要么依赖固定报表，都很难支持自然语言驱动的灵活问数和分析。

#### 第二层：再讲现有方法为什么不够

虽然大语言模型在 Text-to-SQL 和自动分析方面已经很强，但直接放到政务场景会遇到两个突出问题：

1. 查询阶段容易受术语歧义、隐含逻辑和复杂查询结构影响，导致 SQL 不准。
2. 分析阶段如果继续端到端自由生成，又会出现规划缺失、职责混乱、结果不规范等问题。

#### 第三层：接着讲我的总体思路

因此，我没有把问题简单地做成“一个大模型端到端全包”，而是把它拆成两层：

1. 先做可靠查询，保证底层数据拿得对。
2. 再做规范分析，保证上层结果讲得清。

也就是“先查准，再分析；先解决数据正确性，再解决分析规范性”。

#### 第四层：最后讲两个研究点各自回答了什么问题

第一个研究点回答的是：**政务场景下，自然语言怎样更可靠地转成 SQL。**

第二个研究点回答的是：**在可靠取数的基础上，自然语言分析需求怎样被转成规范、稳定、可展示的分析结果。**

这样讲下来，整篇论文就不是两个分散的小点，而是一条完整主线：**面向政务智能问数，构建从自然语言查询到规范化数据分析的完整技术链路。**

---

## 4. 可以直接讲的简洁版本

如果导师时间比较紧，可以直接按下面这版来讲：

“我的论文主要是做面向政务场景的智能问数与数据分析。研究背景是，政务系统里已经沉淀了大量结构化数据，但目前的数据使用方式仍然比较依赖技术人员写 SQL 或固定报表，业务人员很难直接灵活地用自然语言获取分析结果。

围绕这个问题，我认为核心困难有两个。第一个困难是查数不准，因为政务领域有大量专业术语、缩写字段和隐含计算逻辑，比如同比增长率、预算执行率这类指标并不是表里直接存的。第二个困难是，即便查到了数据，如何把开放式分析需求转成规范的趋势分析、对比分析、排名分析和结构分析，现有方法也做得不够稳定。

所以我的论文实际上围绕两个研究点展开。第一个研究点是政务数据查询生成方法，重点解决自然语言到 SQL 的可靠映射问题。我提出了 LA-Schema，把业务术语映射、逻辑公式和值域约束显式注入模式表示，同时设计了双路径异构生成、执行反馈修复和候选判别机制，提升 SQL 生成的准确率和鲁棒性。

第二个研究点是政务数据分析方法，重点解决查到数据以后如何规范分析的问题。我提出了 AP-Schema，把分析需求转成结构化规划，并通过查询增强的任务分解，把复杂业务口径交给前面的查询模块，把排序、占比、透视这些轻量操作留给分析层，从而生成更规范的图表、表格和文字洞察。

这两个研究点之间是递进关系。第三章先解决‘查准’，第四章再解决‘分析规范’，第五章再把这两部分整合成一个智能问数系统。也就是说，我这篇论文不是只做了一个查询模型，也不是只做了一个分析 Agent，而是尝试构建从自然语言问数到规范化分析输出的完整链路。”

---

## 5. 如果导师追问“创新点是什么”，可以这样回答

1. 在查询层，提出了面向政务场景的 LA-Schema，把业务术语映射、逻辑公式和值域约束显式注入模式表示，不再完全依赖大模型隐式推理。
2. 在查询生成层，提出了逻辑映射生成器与渐进分治生成器的异构协同策略，并结合执行反馈修复和候选判别，提高复杂政务查询的准确性和鲁棒性。
3. 在分析层，提出了 AP-Schema，把开放式分析需求转成可校验、可执行的结构化分析规划。
4. 在任务协同层，提出了查询增强的分析任务分解机制，明确了查询层和分析层的职责边界，形成“先查准、再分析”的方法链路。
5. 在系统层，完成了面向政务场景的智能问数系统实现，把方法真正组织成可交互、可追溯的应用闭环。

---

## 6. 最后一句收束

如果要用一句话收束整篇论文，我建议这样讲：

**这篇论文的核心贡献，不是单点提升了某个模型指标，而是围绕政务场景，把“自然语言问数”这件事从可靠查询、规范分析到系统落地，做成了一条完整、可执行、可验证的技术链路。**