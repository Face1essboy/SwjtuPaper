# 导师组会高频问答 20 题

这份问答统一按一个模板来答：

- **一句话结论**：先给结论，不绕。
- **证据点**：只引用论文里已经出现的章节、图表、数字。
- **边界收口**：主动把结论收回到“原型验证范围”。
- **坑点提醒**：提醒自己不要把答案说偏。

---

## 一、研究意义与创新性

### 1. 这个题目是不是一个老问题？为什么还有必要研究？

- 一句话结论：这不是简单重复 Text-to-SQL 老问题，而是面向政务场景，把“可靠查询 + 规范分析 + 原型闭环”贯通起来的问题。
- 证据点：第一章指出政务场景的核心困难不只是生成 SQL，还包括术语映射、隐含口径、分析规划和查询分析协同。
- 证据点：第三章解决“查准”，第四章解决“分析规范”，第五章再把前两章组织成原型闭环，这三个层次不是现有工作同时覆盖的。
- 证据点：实验上不仅看查询 EX，还看分析的 PSR、TCR、RA 和 CA，说明研究目标已经超出了单纯 Text-to-SQL。
- 边界收口：所以这篇论文不是去重复做一个通用 SQL 生成器，而是针对政务场景补足从查询到分析之间缺失的中间链路。
- 坑点提醒：不要答成“这个问题以前没人做过”，那样很容易被导师反问相关工作。

### 2. 你这篇论文是不是本质上只是在“用大模型做问数”？

- 一句话结论：不是，大模型在这里只承担语义理解和规划能力，关键提升来自结构化约束和领域知识注入。
- 证据点：第三章的核心改造是 LA-Schema、双生成器、执行反馈修复和候选判别，而不是只换一个更大的模型。
- 证据点：第四章的核心改造是 AP-Schema、查询增强任务分解和轻量分析算子链，而不是直接让模型自由写分析。
- 证据点：消融实验里，去掉 LA-Schema 后 EX 从 91.47% 降到 84.62%，去掉任务分解后 RA 从 95.8% 降到 86.4%，说明性能主要依赖结构化设计。
- 边界收口：所以本文更准确的说法是“以大语言模型为基础的结构化查询与分析方法”，而不是“大模型直接包办问数”。
- 坑点提醒：不要把卖点说成“模型很强”，导师一旦不认可大模型，这条线就会很被动。

### 3. 与传统 BI 看板或固定报表相比，你的方法价值到底在哪里？

- 一句话结论：BI 和固定报表解决的是固定口径展示，而本文解决的是自然语言驱动的灵活问数与规范分析。
- 证据点：第一章已经明确，传统模式要么依赖技术人员写 SQL，要么依赖固定报表，前者门槛高、周期长，后者灵活性不足。
- 证据点：第四章覆盖趋势、对比、排名、结构四类典型分析任务，说明目标不是只看一张静态表，而是围绕任务做进一步分析。
- 证据点：第五章原型系统保留查询结果、图表、洞察和日志，说明它不是纯展示界面，而是一个从需求到分析结果的闭环。
- 边界收口：所以本文不是替代所有 BI 系统，而是补足固定报表难以支持的自然语言灵活问数和分析能力。
- 坑点提醒：不要贬低 BI 本身，重点讲两者解决的问题不同。

### 4. 与现有 Text-to-SQL 或 Agent 方法相比，你的创新点在哪里？

- 一句话结论：本文的创新不在单个模型，而在查询层、分析层、协同层和系统层的完整设计。
- 证据点：查询层提出 LA-Schema，把业务术语映射、逻辑公式和值域约束显式注入模式表示。
- 证据点：生成层提出逻辑映射生成器与渐进分治生成器，并结合执行反馈修复和候选判别模型。
- 证据点：分析层提出 AP-Schema 和查询增强的任务分解机制，把查询层和分析层职责分清楚。
- 边界收口：如果用一句话概括，本文不是做了一个更会写 SQL 的模型，而是围绕政务问数建立了一条从可靠查询到规范分析的技术链路。
- 坑点提醒：不要把创新点说散，答题时最好固定成“四层创新”这一个结构。

---

## 二、数据与实验可信性

### 5. GovSQL 是怎么构建的？它的可信性如何保证？

- 一句话结论：GovSQL 不是完全自动合成数据，而是按“多源采集、逻辑注入、人机协同混合标注”流程构建的垂直评测集。
- 证据点：第三章数据集统计明确给出 GovSQL 包含 13 个数据库、92 张表、563 个字段、1368 条问题-SQL 对，其中 38.5% 是复杂查询。
- 证据点：数据来源于某市经济开发区政务平台，论文中说明了对敏感信息进行脱敏处理，并显式注入 40 余个核心业务指标的逻辑知识。
- 证据点：问答对先由大模型生成种子问题和初始 SQL，再由具备政务背景的标注人员核验和纠正，训练测试按 7:3 划分。
- 边界收口：因此 GovSQL 的定位是面向政务场景的方法评测集，不是一个完全公开的通用 benchmark。
- 坑点提醒：不要说成“完全人工从零标注”，也不要说成“完全自动生成”，真实说法是人机协同混合标注。

### 6. 为什么主要在同源政务场景上验证？泛化性怎么证明？

- 一句话结论：同源政务场景是为了先验证方法在高业务约束场景下是否有效，而泛化能力通过公共数据集实验做了补充说明。
- 证据点：第三章在 GovSQL 上做到 EX 91.47%，说明方法在目标场景内确实有效。
- 证据点：同时在公共数据集上，BIRD 达到 71.23%，Spider 达到 89.25%，说明方法并不只在自建数据上有效。
- 证据点：结论与展望部分也明确承认，跨地区、跨部门、跨制度口径的泛化能力仍需要进一步验证。
- 边界收口：所以本文的结论是“在同源政务场景中验证有效，并在公共数据集上保持竞争力”，而不是“已经彻底解决所有场景泛化”。
- 坑点提醒：不要硬说“泛化完全没问题”，导师如果追问跨地区制度差异，很容易被卡住。

### 7. 标注过程中用了大模型，会不会存在数据泄漏或者自举偏差？

- 一句话结论：大模型参与的是种子样本生成，不等于评测泄漏，论文在训练划分和实验流程上做了明确隔离。
- 证据点：第三章明确说明，训练集和测试集按 7:3 划分，测试集用于端到端评估。
- 证据点：动态样例库在实验评估阶段关闭在线增量更新，目的是避免把测试数据反向写回样例库造成泄漏。
- 证据点：最终 SQL 和分析任务参考结果都经过人工核验，而不是直接把模型输出当成 gold label。
- 边界收口：所以这里的大模型更像“标注辅助工具”，最终评测仍以人工核验后的标准答案为准。
- 坑点提醒：不要把“用了大模型辅助标注”和“模型知道答案”混在一起说。

### 8. 你的基线和评价指标设置公平吗？为什么这些指标合理？

- 一句话结论：基线和指标是按任务类型分别设置的，公平性主要通过统一基础模型、统一数据库环境和统一评价口径来保证。
- 证据点：第三章中除开源基础模型和 OmniSQL 外，其余基线都使用与本文相同的 Qwen3-Coder-30B-A3B-Instruct，并使用同一数据库与知识库环境。
- 证据点：第四章中 Code-Gen Agent 和 ReAct Agent 也使用与本文相同的基础模型和数据库环境，以保证分析实验可比较。
- 证据点：查询任务用 EX，分析任务用 PSR、TCR、RA、CA，规划质量再单独用 IA、SC、CSR，这和各章节目标是一致的。
- 边界收口：所以本文不是用一个指标硬评所有能力，而是按查询、分析和规划三个层次分别评价。
- 坑点提醒：不要把所有实验混成一个总指标去解释，导师会觉得评价口径不清。

---

## 三、方法设计合理性

### 9. 为什么一定要 LA-Schema？普通 DDL Schema 或 M-Schema 不够吗？

- 一句话结论：在政务场景里，普通 Schema 只能告诉模型表和列长什么样，不能把业务术语、逻辑公式和值域约束显式表达出来。
- 证据点：第三章给出 DDL Schema、M-Schema 和 LA-Schema 的对比示例，LA-Schema 额外提供了 Business Term、Value Term 和 Virtual Columns。
- 证据点：在消融实验中，去掉 LA-Schema 后 EX 从 91.47% 下降到 84.62%，是第三章降幅最大的模块之一。
- 证据点：第一章和第三章都强调，政务场景里很多指标并不是直接字段，而是隐含口径和公式，这正是 LA-Schema 需要解决的问题。
- 边界收口：所以 LA-Schema 的作用不是“让提示更长”，而是把普通模式里表达不出来的业务逻辑显式化。
- 坑点提醒：不要答成“因为我设计了一个新 Schema 所以有创新”，重点要讲它解决了什么具体问题。

### 10. 为什么要双生成器？单一路径生成不行吗？

- 一句话结论：因为政务查询复杂度是长尾分布，单一路径很难同时兼顾高频标准查询和低频复杂查询。
- 证据点：第三章明确把逻辑映射生成器定位为处理高频、标准化业务指标查询，把渐进分治生成器定位为处理复杂长尾查询。
- 证据点：消融实验显示，去掉 Logical Generator 后 EX 降到 87.93%，去掉 DC Generator 后降到 88.88%，两者都不可少。
- 证据点：生成器互补性分析表明，14.6% 的测试问题仅由逻辑映射生成器覆盖，11.1% 仅由渐进分治生成器覆盖，说明二者确实互补。
- 边界收口：所以双生成器不是为了“堆结构”，而是为了适配不同类型查询，扩大正确候选覆盖面。
- 坑点提醒：不要说成“两个生成器只是为了多采样几次”，重点是推理范式不同、覆盖类型不同。

### 11. 为什么要候选判别模型？直接多数投票不行吗？

- 一句话结论：多数投票只能看候选一致性，但复杂业务查询里“最一致的答案”不一定是正确答案。
- 证据点：第三章消融实验中，去掉 Selection Agent 改用一致性多数投票后，EX 从 91.47% 降到 86.12%，下降了 5.35 个百分点。
- 证据点：判别模型微调后，二分类准确率达到 72.85%，明显高于未微调模型的 55% 到 59% 水平。
- 证据点：论文分析指出，很多错误候选在表面结构上和正确 SQL 很接近，但在筛选边界、统计口径或 JOIN 关系上存在关键偏差，这种情况下多数投票很难发现。
- 边界收口：因此判别模型的价值在于识别“看起来很像但业务上不对”的候选，而不是简单重复投票。
- 坑点提醒：不要把 72.85% 说成“判别已经完全可靠”，要说明它通过全局聚合和双向比较进一步降低了局部误判影响。

### 12. 为什么需要 AP-Schema？不能让模型直接做分析吗？

- 一句话结论：开放式分析需求里隐藏了很多约束，如果不先结构化规划，模型很容易漏掉关键步骤或者把分析做偏。
- 证据点：第四章把 AP-Schema 明确定义为分析类型、指标、维度、筛选条件、粒度、操作分层、可视化规格和摘要要求的统一结构。
- 证据点：规划质量评估中，本文方法的 IA、SC、CSR 分别达到 94.6%、91.8%、91.1%，明显高于直接生成的 78.6%、67.3%、53.6%。
- 证据点：在总体对比中，Plan+Free-SQL 比 Direct-Analysis 的 PSR 提升 17.9 个百分点，TCR 提升 17.8 个百分点，说明光引入结构化规划就已经有明显增益。
- 边界收口：所以 AP-Schema 的作用不是增加一个中间术语，而是把原本隐含在自然语言里的分析约束显式化。
- 坑点提醒：不要把 AP-Schema 讲成“一个格式化输出模板”，它本质上是分析规划模式。

### 13. 为什么要把查询层和分析层分开？直接一个 Agent 做到底不行吗？

- 一句话结论：如果查询和分析不分层，查询错误会在分析阶段被再次引入，分析结果的口径也难以保持一致。
- 证据点：第四章明确规定，涉及统计口径、字段绑定、复杂关联和逻辑指标的操作归查询层，结果组织、排序、占比、标注等操作归分析层。
- 证据点：消融实验中，去掉任务分解后，RA 从 95.8% 降到 86.4%，说明分层对结果正确性影响非常明显。
- 证据点：Plan+Free-SQL 虽然已经有 AP-Schema，但因为分析阶段自由生成 SQL，TCR 和 RA 仍明显低于完整方法。
- 边界收口：所以分层的目的不是让流程更复杂，而是把“数据正确性”和“结果表达性”分别放到最合适的位置上保证。
- 坑点提醒：不要把分层解释成纯工程拆分，重点要讲它和统计口径一致性、结果正确性直接相关。

---

## 四、系统与落地可行性

### 14. 既然方法已经验证了，为什么还要做系统？

- 一句话结论：系统章节的作用不是额外追求一个工程亮点，而是验证前两章方法能否被组织成统一可用的闭环。
- 证据点：第五章明确把系统能力概括为自然语言问数、可靠查询生成、自动化分析生成和结果留痕追溯四部分。
- 证据点：系统把第三章查询子图和第四章分析子图串起来，验证从自然语言输入到结果包输出的链路可以落地组织。
- 证据点：原型界面展示了查询结果、分析图表、洞察和追溯信息，说明前两章不是只停留在离线实验。
- 边界收口：因此第五章的定位是“原型系统和方法闭环验证”，而不是“已经完成大规模生产部署”。
- 坑点提醒：不要主动把这一章讲成成熟项目落地，否则导师很可能转而追问部署范围和上线细节。

### 15. 为什么系统里选 FastAPI 和 LangGraph？

- 一句话结论：这两个组件分别对应“统一服务入口”和“查询/分析工作流编排”两个最核心的工程需求。
- 证据点：第五章说明 FastAPI 负责 HTTP 接入、Pydantic 校验、同步查询和异步分析接口组织，以及统一异常处理。
- 证据点：LangGraph 的 StateGraph 用来组织查询子图和分析子图，使分析请求能先生成 AP-Schema，再投影查询约束，最后复用查询能力。
- 证据点：这个选择和论文方法是一一对应的，因为第四章本身就是一个多阶段、有状态的协同流程。
- 边界收口：这里的选型重点不在于“用了什么流行框架”，而在于它是否能把论文中的方法流程稳定组织起来。
- 坑点提醒：不要答成“因为它们先进”或“社区流行”，那不是论文视角下的理由。

### 16. 为什么数据层同时用了 PostgreSQL、Milvus 和 Redis？

- 一句话结论：因为三类数据对象完全不同，结构化元数据、向量检索对象和热状态缓存不适合混在一个存储里处理。
- 证据点：第五章明确说明 PostgreSQL 用于系统元数据、领域知识库、任务和会话状态等结构化对象。
- 证据点：Milvus 用于字段值向量和样例向量索引，对应第三章的值检索和动态样例检索需求。
- 证据点：Redis 用于热点缓存、任务热状态和 SSE 事件通道，对应流式回传和性能优化需求。
- 边界收口：因此这里不是为了“堆组件”，而是分别承接结构化存储、向量检索和运行态缓存三类不同职责。
- 坑点提醒：不要说成“这是工业界标准架构”，只要回到论文中的方法需求和系统职责即可。

### 17. 为什么查询做同步、分析做异步？

- 一句话结论：因为查询和分析的时长、输出形态和交互预期不同，同步查询和异步分析更符合两类任务的实际特征。
- 证据点：第五章说明查询接口在请求上下文中直接执行查询子图并返回 QueryResponse，同时可支持 SSE 流式进度推送。
- 证据点：分析任务需要先生成 AP-Schema，再取数，再做图表、表格和洞察组织，链路更长，所以采用“先建任务、后执行”的异步模式。
- 证据点：这种设计和系统统一状态机 PENDING、RUNNING、SUCCESS、FAILED 是配套的，便于结果回放和异常处理。
- 边界收口：所以这不是人为复杂化，而是根据任务时长和结果组织复杂度做的区分。
- 坑点提醒：不要把同步和异步讲成“技术偏好”，要讲成与任务类型直接匹配。

### 18. 结果追溯和人工复核是怎么做的？

- 一句话结论：追溯能力来自任务状态、执行日志和结果产物的结构化保存，而不是事后手工回忆流程。
- 证据点：第五章明确设计了 query_task、analysis_task、result_artifact、execution_log 等核心表，用来保存任务主记录、结果产物和执行日志。
- 证据点：对于分析任务，系统会保存 AP-Schema、图表、表格、洞察和日志；对于查询任务，会保存最终 SQL、结果表和执行状态。
- 证据点：界面上还能展示分析规划、执行轨迹以及使用的数据与知识信息，支持后续复核和追问。
- 边界收口：因此系统的追溯能力主要是验证“结果从哪里来、经过了什么步骤”，并不意味着已经覆盖真实政务场景中的全部审计要求。
- 坑点提醒：不要把“可追溯”说成“已经满足正式审计标准”，论文没有做到这一步。

---

## 五、局限与扩展

### 19. 知识库维护成本高、权限控制复杂，这个系统怎么走向真实应用？

- 一句话结论：这两个问题论文已经明确承认是当前边界，现阶段的结论是“方法在原型范围内成立”，而不是“真实治理问题已经完全解决”。
- 证据点：结论部分明确指出，领域知识库构建、动态样例整理和部分训练标注仍依赖人工参与，自动更新与自适应能力不足。
- 证据点：未来展望中专门提出要研究更自动化的知识获取、知识更新与样例演化机制，以降低维护成本。
- 证据点：结论还明确指出，对更严格的安全权限约束控制尚未展开系统研究，这部分属于后续工作。
- 边界收口：因此真实应用要再往前走，重点不是再堆一个模型，而是要把知识更新机制和权限治理机制补上。
- 坑点提醒：不要为了显得“落地”而硬说权限和安全已经做完，最稳妥的说法是“论文已识别问题，但不把它伪装成已经解决”。

### 20. 你这篇论文最大的不足是什么？下一步准备怎么做？

- 一句话结论：最大的不足是验证范围仍偏同源场景，知识维护仍有人工成本，复杂分析和真实部署治理还没有完全展开。
- 证据点：结论部分第一条不足就是数据与任务验证范围有限，当前主要集中在同源政务业务场景。
- 证据点：第二条不足是知识库和样例库的人工维护成本较高，动态业务规则变化下自动更新能力不足。
- 证据点：第三条不足是当前重点覆盖趋势、对比、排名和结构四类典型任务，对多轮深度分析、预测分析、复杂权限控制仍未展开。
- 边界收口：下一步我会沿三条线推进：一是构建更大规模、多区域的数据集；二是研究更自动化的知识更新；三是扩展多轮分析、复杂洞察和权限控制能力。
- 坑点提醒：不要答成“这篇论文已经没有明显不足”，主动承认边界反而更稳。

---

## 最后再记住这 5 句高频收束句

1. **本文不是让大模型直接替代人工分析，而是在结构化约束下完成可靠查询与规范分析。**
2. **第三章解决的是查准，第四章解决的是分析规范，第五章只是把前两章组织成原型闭环。**
3. **GovSQL 上的 91.47% 和分析任务上的 95.8% 结果正确率，说明方法在目标场景里是有效的。**
4. **公共数据集上的 71.23% 和 89.25% 说明方法并不只在自建数据上有效，但更大范围泛化仍需继续验证。**
5. **系统部分的定位是原型实现和方法闭环验证，不把它讲成真实大规模生产部署。**

